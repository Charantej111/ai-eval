import { Link } from 'react-router-dom';
import { motion } from 'framer-motion';
import { ShieldAlert, ArrowRight, Lock } from 'lucide-react';

const steps = [
  { n: '01', title: 'Six Prompts', desc: 'You will evaluate 6 different festival poster prompts, one at a time.' },
  { n: '02', title: 'Three Posters Each', desc: 'Every prompt shows 3 posters generated by different AI systems. The model identities are hidden to ensure unbiased evaluation.' },
  { n: '03', title: 'Seven Rating Criteria', desc: 'Rate each poster on 7 criteria: Prompt Adherence, Cultural Accuracy, Language Correctness, Text Readability, Visual Appeal, Business Usability, and Overall Preference.' },
  { n: '04', title: 'Complete All Ratings', desc: 'You must rate all 7 criteria for all 3 posters before proceeding to the next prompt.' },
  { n: '05', title: 'Auto‑Save', desc: 'Your ratings are saved instantly as you click. You can go back to a previous prompt and re-rate at any time.' },
];

export default function InstructionsPage() {
  const containerVariants = {
    hidden: {},
    visible: {
      transition: {
        staggerChildren: 0.08,
      },
    },
  };

  const itemVariants = {
    hidden: { opacity: 0, x: -10 },
    visible: { opacity: 1, x: 0, transition: { duration: 0.5, ease: 'easeOut' as const } },
  };

  return (
    <div className="min-h-screen flex items-center justify-center p-6 py-12 dashboard-gradient-bg">
      <motion.div
        initial={{ opacity: 0, y: 15 }}
        animate={{ opacity: 1, y: 0 }}
        transition={{ duration: 0.6, ease: [0.16, 1, 0.3, 1] }}
        className="max-w-md w-full glass-card p-6 md:p-8 space-y-6 rounded-[24px]"
      >
        <div className="space-y-1">
          <h1 className="text-[22px] font-bold tracking-tight text-[#171717]">Study Instructions</h1>
          <p className="text-[12px] font-medium text-[#2563EB]">
            Step 2 of 3 · Guidelines
          </p>
        </div>

        <motion.div
          variants={containerVariants}
          initial="hidden"
          animate="visible"
          className="space-y-4"
        >
          {steps.map(({ n, title, desc }) => (
            <motion.div
              key={n}
              variants={itemVariants}
              className="flex gap-4 p-3 rounded-[14px] hover:bg-white/40 backdrop-blur-xs transition duration-200"
            >
              <div className="flex-shrink-0 w-8 h-8 rounded-full text-xs font-semibold flex items-center justify-center bg-white/40 text-[#2563EB] border border-white/50 shadow-xs">
                {n}
              </div>
              <div className="space-y-0.5">
                <div className="text-sm font-semibold text-[#171717]">{title}</div>
                <div className="text-xs leading-relaxed text-[#6B7280]">{desc}</div>
              </div>
            </motion.div>
          ))}
        </motion.div>

        <div className="pt-4 border-t border-white/50 space-y-4">
          <div className="flex items-center justify-center gap-1.5 text-xs font-medium text-[#9CA3AF]">
            <Lock className="w-3.5 h-3.5 shrink-0" />
            <span>Ratings are anonymous and securely saved.</span>
          </div>

          <motion.div whileHover={{ y: -1 }} whileTap={{ scale: 0.98 }}>
            <Link
              to="/evaluate/1"
              className="w-full inline-flex items-center justify-center text-white font-medium text-[14px] py-3 transition-all"
              style={{ backgroundColor: '#2563EB', borderRadius: '12px', boxShadow: '0 1px 3px rgba(37,99,235,0.3)' }}
            >
              <span className="flex items-center justify-center gap-1.5">
                <span>Begin Evaluation</span>
                <ArrowRight className="w-4 h-4" />
              </span>
            </Link>
          </motion.div>
        </div>
      </motion.div>
    </div>
  );
}
